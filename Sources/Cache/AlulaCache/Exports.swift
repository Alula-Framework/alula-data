/// One import for consumers: `import AlulaCache` carries the Core surface
/// the annotations sit beside (`@Service`, `@Inject`, `Container`,
/// `Configuration`) — same posture as AlulaDataValkey's exports.
@_exported import AlulaCore
