/// One import for consumers: `import AlulaRateLimitValkey` carries
/// AlulaRateLimit — the seam this adapter implements — same posture as the
/// cache and session adapters.
@_exported import AlulaRateLimit
