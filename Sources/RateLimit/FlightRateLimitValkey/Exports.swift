/// One import for consumers: `import FlightRateLimitValkey` carries
/// FlightRateLimit — the seam this adapter implements — same posture as the
/// cache and session adapters.
@_exported import FlightRateLimit
