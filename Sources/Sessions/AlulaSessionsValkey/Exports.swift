/// One import for consumers: `import AlulaSessionsValkey` carries
/// AlulaSessions — the seam this adapter implements — same posture as the
/// cache adapter's exports.
@_exported import AlulaSessions
