/// One import for consumers: `import FlightSessionsValkey` carries
/// FlightSessions — the seam this adapter implements — same posture as the
/// cache adapter's exports.
@_exported import FlightSessions
